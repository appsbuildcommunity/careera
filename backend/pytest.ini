[pytest]
testpaths = app
pythonpath = .
addopts = --import-mode=importlib -m "not integration"
markers =
    integration: tests that make real external LLM API calls (require LLM_API_KEY); run with pytest -m integration
